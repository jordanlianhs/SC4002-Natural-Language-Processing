# SC4002
Natural Language Processing

To run the file in the ipynb, you need to have these packages installed in your python or python environment.
just run the following command in your terminal or cmd.

pip install -r requirements.txt

the results of our model from the report are loaded in the ipynb file.
```
